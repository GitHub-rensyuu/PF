<div class="container">
  <div class="row">
    <div class='col-md-12'>
          <% unless @customers.present? %>
            <h1><%#= @customer.nickname %>さんを通報している人はいません。</h1>
          <% else %>
            <h1>この人を通報している人</h1>
            <%= render 'report',customers: @customers %>
          <% end %>
    </div>
  </div>
  <%#= paginate @customers %>
</div>