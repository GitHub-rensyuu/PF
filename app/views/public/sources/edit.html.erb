<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <%= render 'layouts/errors', obj: @source %>
  		<h1>投稿内容編集</h1>
  		<%= render 'form', source: @source %>
  		<%= link_to 'Show', @source, class: "source_#{@source.id}" %> |
  		<%= link_to 'Back', sources_path, class: "back" %>
  </div>
</div>